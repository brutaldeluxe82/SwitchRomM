# Required
# SERVER_URL: RomM server base URL (http only)
SERVER_URL=http://192.168.1.100:8080
# USERNAME / PASSWORD: Basic auth credentials (leave empty if not needed)
USERNAME=
PASSWORD=
# DOWNLOAD_DIR: Base cache folder on SD; platform/romId subfolders are created automatically
# Empty derives from OUTPUT_LAYOUT (tico -> sdmc:/tico/roms, retroarch -> sdmc:/retroarch/downloads)
DOWNLOAD_DIR=sdmc:/romm_cache
# OUTPUT_LAYOUT: tico or retroarch. Controls default SD folder layout.
OUTPUT_LAYOUT=tico
# BIOS_DIR: base BIOS folder; empty derives from OUTPUT_LAYOUT (tico -> sdmc:/tico/system, retroarch -> sdmc:/retroarch/system)
BIOS_DIR=

# Optional
# API_TOKEN: Reserved for future token-based auth
API_TOKEN=
# HTTP_TIMEOUT_SECONDS: HTTP recv/send timeout per request
HTTP_TIMEOUT_SECONDS=30
# FAT32_SAFE: Enable FAT32-safe splitting (default true; downloader always splits large files)
FAT32_SAFE=true
# LOG_LEVEL: debug|info|warn|error (default info)
LOG_LEVEL=info
# SPEED_TEST_URL: Display of console DL SPD for 40MB payload from URL upon app launch
SPEED_TEST_URL=
